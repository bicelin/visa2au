# Cloudflare Pages Configuration
# This file is used by Wrangler for Cloudflare Pages deployments

name = "visa2au"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# Build settings
build_command = "npm run build"
build_output_dir = "dist"

# Environment variables
[vars]
NODE_VERSION = "20"

# Routes (for reference - configure in Cloudflare Dashboard)
# test.visa2.au will be configured manually in the Cloudflare Pages dashboard
